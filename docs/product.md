# Product

MVP: multi-tenant field assistant for pest-control techs in Alberta multi-unit housing. Offline-first workflow with pack-driven, compliant IPM plans and auditable reports. ML is assistive only.

Key user flows:
- Start visit -> capture evidence -> select pest -> confirm -> plan -> report -> sync
